version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm uninstall tsup
      - npm install -g typescript
      - npm install -g tsup
      - npm ci
  
  build: 
    commands:
      - npm run build

artifacts:
  files:
    - package.json
    - 'dist/**/*'
    - '.platform/**/*'      # AWS EB config files
    - '.ebextensions/**/*' # AWS EB config files
  discard-paths: no